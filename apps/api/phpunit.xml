<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <!-- force="true" จำเป็น: compose ส่ง CACHE_STORE=redis เป็น env จริง
             ซึ่งชนะค่าใน phpunit.xml ทำให้ rate limiter และ nonce ไปใช้ Valkey ของ dev
             แล้ว state ค้างข้ามเทสต์และข้ามการรัน -->
        <env name="CACHE_STORE" value="array" force="true"/>
        <!-- ต้องเป็น Postgres ไม่ใช่ sqlite: schema พึ่ง jsonb, partial unique index
             และ trigger ของ audit_logs ซึ่ง sqlite ไม่มี -->
        <env name="DB_CONNECTION" value="pgsql" force="true"/>
        <!-- คนละฐานกับ dev: RefreshDatabase migrate ใหม่ทุกครั้ง ถ้าชี้ฐานเดียวกัน
             `make test` จะลบข้อมูล dev ทิ้งทุกรอบ -->
        <env name="DB_DATABASE" value="mvp_test" force="true"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync" force="true"/>
        <env name="SESSION_DRIVER" value="array" force="true"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
